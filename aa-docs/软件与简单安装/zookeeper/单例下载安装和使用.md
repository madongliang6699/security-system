
## 下载
https://archive.apache.org/dist/zookeeper/zookeeper-3.6.2/
apache-zookeeper-3.6.2-bin.tar.gz
已经下载好了在阿里云。

## 安装 
上传到Linux系统的指定文件夹。  
tar -zxvf apache-zookeeper-3.6.2-bin.tar.gz

## 单例启动的简单配置
复制一份conf文件夹下的zoo_sample.cfg文件改名为zoo.cfg, 因为zookeeper启动默认是找zoo.cfg名字的配置文件为启动配置。  
修改配置文件的内容：
dataDir=/tmp/zookeeper（修改为自己的目录）
dataLogDir=/tmp/zookeeper（修改为自己的目录）

主要修改项为dataDir和dataLogDir，dataDir是zookeeper存放数据的地方，dataLogDir是存放zookeeper日志的地方。
连接端口可以默认2181
如果只配置dataDir，则数据和日志都会创建在dataDir目录下。

默认情况下zookeeper会占有8080端口，如果你不想8080端口被占用，增加一行admin.serverPort=8082，指定你自己的端口。
如果使用8080端口，那访问 http://192.168.61.12:8080/commands 地址可以查看zk的一些信息。

其他配置项的意思我们留到后面再说。  



## 启动
bin目录下：./zkServer.sh start  后台运行
bin目录下：./zkServer.sh start-foreground  非后台运行
bin目录下：./zkServer.sh status  查看启动状态
bin目录下: ./zkServer.sh stop 停止ZK服务
bin目录下: ./zkServer.sh restart 重启ZK服务


## 客户端连接
bin目录下： ./zkCli.sh 默认连接本机2181的zk服务
bin目录下： ./zkCli.sh -server 127.0.0.1:2181  指定连接的服务

## 开放防火墙端口
firewall-cmd --state
firewall-cmd --add-port=2181/tcp --permanent
firewall-cmd --reload
然后就可以用客户端连接了

## zk命令
ls /