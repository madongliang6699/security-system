##下面的内容是基于CentOS 7系统

## 安装vim
yum install vim

## 安装htop
首先在CentOS 7上启用epel版本：
yum -y install epel-release
安装CentOS htop：
yum -y install htop

## 安装jdk
查询可安装的jdk版本：
yum search java|grep jdk
安装指定的版本：
yum install java-1.8.0-openjdk-src.x86_64


## 安装git 
如果需要的话就安装，比如Jenkins底层需要使用git拉取代码，Jenkins服务器上要安装git。  
yum install git -y

## 安装maven
安装maven前需要先安装jdk

yum install maven -y

## 安装 lsof
yum update（这个不要随意执行，不然会更新很久已有的软件）
yum install -y lsof
lsof -v
 
