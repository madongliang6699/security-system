

## 定位进程的路径
比如查看到一个进程占用很高的cpu，这个进程可能是病毒，或代码有问题，怎么知道这个进程是在哪的，怎么启动的呢？

可以先拿到他的PID，

然后去/proc文件夹（进程文件夹，每个运行的进程都会分配内存，这个/proc文件夹就是存放正在运行的进程信息的文件夹），
这个文件夹下找到和PID号相同的文件夹，该文件夹中存的就是当前进程的运行信息，
其中可以看到一个exe的软连接文件，这个软连接指向的就是该进程下的命令所在地。就找到了进程的程序。
其中还有一个fd的文件夹，记录了当前程序使用到的文件。 